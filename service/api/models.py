from django.db import models

# Create your models here.
#class Player(models.Model):
#    userName = models.CharField(max_length=25, unique=True)
#    passwordHash = models.CharField(default=0, max_length=255)
#
#    def __str__(self):
#        return self.name